//The Ping-Pong method counts from 1 to a given number

//If it's divisible by 3, then return ping (6 should return ping)

//If it's divisible by 5, then return pong (5 should return pong).

//If it's divisible by 3 and 5, then return pingpong (15 should return pingpong).
